## Copy this file to secrets.toml and fill in real values.
## secrets.toml itself must never be committed (see .gitignore) -- this
## .example file is the only version that belongs in version control.

# db_url = "postgresql://user:password@host:5432/dbname"
# api_key = "replace-with-real-key"
